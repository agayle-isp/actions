name: Test filters

on:
  push:

jobs:
  filter:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check environment
        run: |
          echo "PATH: ${PATH}"
          which git